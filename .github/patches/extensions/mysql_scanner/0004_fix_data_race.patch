diff --git a/src/include/storage/mysql_catalog_set.hpp b/src/include/storage/mysql_catalog_set.hpp
index 7273b59..83c60a3 100644
--- a/src/include/storage/mysql_catalog_set.hpp
+++ b/src/include/storage/mysql_catalog_set.hpp
@@ -24,7 +24,7 @@ public:
 	optional_ptr<CatalogEntry> GetEntry(ClientContext &context, const string &name);
 	virtual void DropEntry(ClientContext &context, DropInfo &info);
 	void Scan(ClientContext &context, const std::function<void(CatalogEntry &)> &callback);
-	virtual optional_ptr<CatalogEntry> CreateEntry(unique_ptr<CatalogEntry> entry);
+	virtual optional_ptr<CatalogEntry> CreateEntry(ClientContext &context, shared_ptr<CatalogEntry> entry);
 	void ClearEntries();
 
 protected:
@@ -33,6 +33,7 @@ protected:
 	void TryLoadEntries(ClientContext &context);
 
 	void EraseEntryInternal(const string &name);
+	void ClearEntriesLocked();
 
 protected:
 	Catalog &catalog;
@@ -40,7 +41,7 @@ protected:
 private:
 	mutex entry_lock;
 	mutex load_lock;
-	case_insensitive_map_t<unique_ptr<CatalogEntry>> entries;
+	case_insensitive_map_t<shared_ptr<CatalogEntry>> entries;
 	atomic<bool> is_loaded;
 };
 
@@ -48,7 +49,7 @@ class MySQLInSchemaSet : public MySQLCatalogSet {
 public:
 	MySQLInSchemaSet(MySQLSchemaEntry &schema);
 
-	optional_ptr<CatalogEntry> CreateEntry(unique_ptr<CatalogEntry> entry) override;
+	optional_ptr<CatalogEntry> CreateEntry(ClientContext &context, shared_ptr<CatalogEntry> entry) override;
 
 protected:
 	MySQLSchemaEntry &schema;
diff --git a/src/include/storage/mysql_transaction.hpp b/src/include/storage/mysql_transaction.hpp
index 28a1f9f..797e723 100644
--- a/src/include/storage/mysql_transaction.hpp
+++ b/src/include/storage/mysql_transaction.hpp
@@ -30,6 +30,7 @@ public:
 	MySQLConnection &GetConnection();
 	unique_ptr<MySQLResult> Query(const string &query);
 	static MySQLTransaction &Get(ClientContext &context, Catalog &catalog);
+	optional_ptr<CatalogEntry> ReferenceEntry(shared_ptr<CatalogEntry> &entry);
 	AccessMode GetAccessMode() const {
 		return access_mode;
 	}
@@ -46,6 +47,8 @@ private:
 	mutex transaction_state_lock;
 	AccessMode access_mode;
 	string time_zone;
+	mutex referenced_entries_lock;
+	reference_map_t<CatalogEntry, shared_ptr<CatalogEntry>> referenced_entries;
 	dbconnector::pool::AcquireMode acquire_mode = dbconnector::pool::AcquireMode::FORCE;
 };
 
diff --git a/src/storage/mysql_catalog_set.cpp b/src/storage/mysql_catalog_set.cpp
index 8b8dd64..6c71bca 100644
--- a/src/storage/mysql_catalog_set.cpp
+++ b/src/storage/mysql_catalog_set.cpp
@@ -9,13 +9,14 @@ MySQLCatalogSet::MySQLCatalogSet(Catalog &catalog) : catalog(catalog), is_loaded
 }
 
 optional_ptr<CatalogEntry> MySQLCatalogSet::GetEntry(ClientContext &context, const string &name) {
+	auto &transaction = MySQLTransaction::Get(context, catalog);
 	TryLoadEntries(context);
 	lock_guard<mutex> l(entry_lock);
 	auto entry = entries.find(name);
 	if (entry == entries.end()) {
 		return nullptr;
 	}
-	return entry->second.get();
+	return transaction.ReferenceEntry(entry->second);
 }
 
 void MySQLCatalogSet::TryLoadEntries(ClientContext &context) {
@@ -23,8 +24,13 @@ void MySQLCatalogSet::TryLoadEntries(ClientContext &context) {
 	if (is_loaded) {
 		return;
 	}
-	is_loaded = true;
-	LoadEntries(context);
+	try {
+		LoadEntries(context);
+		is_loaded = true;
+	} catch (...) {
+		ClearEntriesLocked();
+		throw;
+	}
 }
 
 void MySQLCatalogSet::DropEntry(ClientContext &context, DropInfo &info) {
@@ -52,37 +58,45 @@ void MySQLCatalogSet::EraseEntryInternal(const string &name) {
 }
 
 void MySQLCatalogSet::Scan(ClientContext &context, const std::function<void(CatalogEntry &)> &callback) {
+	auto &transaction = MySQLTransaction::Get(context, catalog);
 	TryLoadEntries(context);
 	lock_guard<mutex> l(entry_lock);
 	for (auto &entry : entries) {
+		transaction.ReferenceEntry(entry.second);
 		callback(*entry.second);
 	}
 }
 
-optional_ptr<CatalogEntry> MySQLCatalogSet::CreateEntry(unique_ptr<CatalogEntry> entry) {
+optional_ptr<CatalogEntry> MySQLCatalogSet::CreateEntry(ClientContext &context, shared_ptr<CatalogEntry> entry) {
+	auto &transaction = MySQLTransaction::Get(context, catalog);
 	lock_guard<mutex> l(entry_lock);
-	auto result = entry.get();
-	if (result->name.empty()) {
+	if (entry->name.empty()) {
 		throw InternalException("MySQLCatalogSet::CreateEntry called with empty name");
 	}
-	entries.insert(make_pair(result->name, std::move(entry)));
-	return result;
+	auto name = entry->name;
+	auto result = entries.emplace(std::move(name), std::move(entry));
+	return transaction.ReferenceEntry(result.first->second);
 }
 
-void MySQLCatalogSet::ClearEntries() {
+void MySQLCatalogSet::ClearEntriesLocked() {
 	lock_guard<mutex> l(entry_lock);
 	entries.clear();
 	is_loaded = false;
 }
 
+void MySQLCatalogSet::ClearEntries() {
+	lock_guard<mutex> l(load_lock);
+	ClearEntriesLocked();
+}
+
 MySQLInSchemaSet::MySQLInSchemaSet(MySQLSchemaEntry &schema) : MySQLCatalogSet(schema.ParentCatalog()), schema(schema) {
 }
 
-optional_ptr<CatalogEntry> MySQLInSchemaSet::CreateEntry(unique_ptr<CatalogEntry> entry) {
+optional_ptr<CatalogEntry> MySQLInSchemaSet::CreateEntry(ClientContext &context, shared_ptr<CatalogEntry> entry) {
 	if (!entry->internal) {
 		entry->internal = schema.internal;
 	}
-	return MySQLCatalogSet::CreateEntry(std::move(entry));
+	return MySQLCatalogSet::CreateEntry(context, std::move(entry));
 }
 
 } // namespace duckdb
diff --git a/src/storage/mysql_index_set.cpp b/src/storage/mysql_index_set.cpp
index 3abbfc1..58b7dbf 100644
--- a/src/storage/mysql_index_set.cpp
+++ b/src/storage/mysql_index_set.cpp
@@ -46,8 +46,8 @@ WHERE TABLE_SCHEMA = ${SCHEMA_NAME};
 		info.schema = schema.name;
 		info.table = table_name;
 		info.index_name = index_name;
-		auto index_entry = make_uniq<MySQLIndexEntry>(catalog, schema, info, table_name);
-		CreateEntry(std::move(index_entry));
+		auto index_entry = make_shared_ptr<MySQLIndexEntry>(catalog, schema, info, table_name);
+		CreateEntry(context, std::move(index_entry));
 	}
 }
 
diff --git a/src/storage/mysql_schema_set.cpp b/src/storage/mysql_schema_set.cpp
index 9760356..a6792f2 100644
--- a/src/storage/mysql_schema_set.cpp
+++ b/src/storage/mysql_schema_set.cpp
@@ -38,8 +38,8 @@ FROM information_schema.schemata
 		CreateSchemaInfo info;
 		info.schema = result->GetString(0);
 		info.internal = MySQLSchemaIsInternal(info.schema);
-		auto schema = make_uniq<MySQLSchemaEntry>(catalog, info);
-		CreateEntry(std::move(schema));
+		auto schema = make_shared_ptr<MySQLSchemaEntry>(catalog, info);
+		CreateEntry(context, std::move(schema));
 	}
 }
 
@@ -48,8 +48,8 @@ optional_ptr<CatalogEntry> MySQLSchemaSet::CreateSchema(ClientContext &context,
 
 	string create_sql = "CREATE SCHEMA " + MySQLUtils::WriteIdentifier(info.schema);
 	transaction.Query(create_sql);
-	auto schema_entry = make_uniq<MySQLSchemaEntry>(catalog, info);
-	return CreateEntry(std::move(schema_entry));
+	auto schema_entry = make_shared_ptr<MySQLSchemaEntry>(catalog, info);
+	return CreateEntry(context, std::move(schema_entry));
 }
 
 } // namespace duckdb
diff --git a/src/storage/mysql_table_set.cpp b/src/storage/mysql_table_set.cpp
index 32761f4..b41ca9c 100644
--- a/src/storage/mysql_table_set.cpp
+++ b/src/storage/mysql_table_set.cpp
@@ -88,8 +88,8 @@ ORDER BY c.table_name, c.ordinal_position;
 		tables.push_back(std::move(info));
 	}
 	for (auto &tbl_info : tables) {
-		auto table_entry = make_uniq<MySQLTableEntry>(catalog, schema, *tbl_info);
-		CreateEntry(std::move(table_entry));
+		auto table_entry = make_shared_ptr<MySQLTableEntry>(catalog, schema, *tbl_info);
+		CreateEntry(context, std::move(table_entry));
 	}
 }
 
@@ -125,10 +125,8 @@ unique_ptr<MySQLTableInfo> MySQLTableSet::GetTableInfo(ClientContext &context, M
 
 optional_ptr<CatalogEntry> MySQLTableSet::RefreshTable(ClientContext &context, const string &table_name) {
 	auto table_info = GetTableInfo(context, schema, table_name);
-	auto table_entry = make_uniq<MySQLTableEntry>(catalog, schema, *table_info);
-	auto table_ptr = table_entry.get();
-	CreateEntry(std::move(table_entry));
-	return table_ptr;
+	auto table_entry = make_shared_ptr<MySQLTableEntry>(catalog, schema, *table_info);
+	return CreateEntry(context, std::move(table_entry));
 }
 
 // FIXME - this is almost entirely copied from TableCatalogEntry::ColumnsToSQL -
@@ -245,8 +243,8 @@ optional_ptr<CatalogEntry> MySQLTableSet::CreateTable(ClientContext &context, Bo
 	auto &transaction = MySQLTransaction::Get(context, catalog);
 	auto create_sql = GetMySQLCreateTable(context, info.Base());
 	transaction.Query(create_sql);
-	auto tbl_entry = make_uniq<MySQLTableEntry>(catalog, schema, info.Base());
-	return CreateEntry(std::move(tbl_entry));
+	auto tbl_entry = make_shared_ptr<MySQLTableEntry>(catalog, schema, info.Base());
+	return CreateEntry(context, std::move(tbl_entry));
 }
 
 void MySQLTableSet::AlterTable(ClientContext &context, RenameTableInfo &info) {
diff --git a/src/storage/mysql_transaction.cpp b/src/storage/mysql_transaction.cpp
index 09f8330..664b74b 100644
--- a/src/storage/mysql_transaction.cpp
+++ b/src/storage/mysql_transaction.cpp
@@ -130,6 +130,13 @@ unique_ptr<MySQLResult> MySQLTransaction::Query(const string &query) {
 	}
 }
 
+optional_ptr<CatalogEntry> MySQLTransaction::ReferenceEntry(shared_ptr<CatalogEntry> &entry) {
+	auto &result = *entry;
+	lock_guard<mutex> guard(referenced_entries_lock);
+	referenced_entries.emplace(result, entry);
+	return result;
+}
+
 MySQLTransaction &MySQLTransaction::Get(ClientContext &context, Catalog &catalog) {
 	return Transaction::Get(context, catalog).Cast<MySQLTransaction>();
 }
