diff --git i/src/storage/postgres_catalog.cpp w/src/storage/postgres_catalog.cpp
index f91c1f2..f0e89c1 100644
--- i/src/storage/postgres_catalog.cpp
+++ w/src/storage/postgres_catalog.cpp
@@ -28,7 +28,7 @@ PostgresCatalog::PostgresCatalog(AttachedDatabase &db_p, string connection_strin
 	this->version = connection.GetConnection().GetPostgresVersion();
 }
 
-string EscapeConnectionString(const string &input) {
+static string EscapeConnectionString(const string &input) {
 	string result = "'";
 	for (auto c : input) {
 		if (c == '\\') {
@@ -57,7 +57,7 @@ string AddConnectionOption(const KeyValueSecret &kv_secret, const string &name)
 	return result;
 }
 
-unique_ptr<SecretEntry> GetSecret(ClientContext &context, const string &secret_name) {
+static unique_ptr<SecretEntry> GetSecret(ClientContext &context, const string &secret_name) {
 	auto &secret_manager = SecretManager::Get(context);
 	auto transaction = CatalogTransaction::GetSystemCatalogTransaction(context);
 	// FIXME: this should be adjusted once the `GetSecretByName` API supports this use case
diff --git i/src/storage/postgres_schema_entry.cpp w/src/storage/postgres_schema_entry.cpp
index 55df388..1d03a62 100644
--- i/src/storage/postgres_schema_entry.cpp
+++ w/src/storage/postgres_schema_entry.cpp
@@ -157,7 +157,7 @@ void PostgresSchemaEntry::Alter(CatalogTransaction transaction, AlterInfo &info)
 	tables.AlterTable(postgres_transaction, alter);
 }
 
-bool CatalogTypeIsSupported(CatalogType type) {
+static bool CatalogTypeIsSupported(CatalogType type) {
 	switch (type) {
 	case CatalogType::INDEX_ENTRY:
 	case CatalogType::TABLE_ENTRY:
